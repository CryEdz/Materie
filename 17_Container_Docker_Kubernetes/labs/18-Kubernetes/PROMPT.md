# Prompt — Kubernetes

Contesto: `17_Container_Docker_Kubernetes/labs/18-Kubernetes`

Obiettivo: scrivi i manifest Kubernetes (Deployment, Service) per esporre un'app web su porta 80 e fornisci i comandi `kubectl apply -f` e `kubectl port-forward` per testare in locale.

Richiedo: manifest YAML, comandi per deploy locale e istruzioni per verificare lo stato delle risorse.
